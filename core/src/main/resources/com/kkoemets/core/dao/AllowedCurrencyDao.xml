<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kkoemets.core.dao.AllowedCurrencyDao">

    <select id="findAll" resultType="com.kkoemets.domain.codes.Currency">
        SELECT ccy
        FROM core.allowed_currency
        WHERE enabled
    </select>

</mapper>
